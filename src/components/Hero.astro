---
const personalImageAlt = "Victor Moreno";
import Badge from "@/components/Badge.astro";
import LinkedInIcon from "@/components/icons/LinkedIn.astro";
import MailIcon from "@/components/icons/Mail.astro";
import SocialPill from "@/components/SocialPill.astro";
import GitHub from "./icons/GitHub.astro";
---

<section
  class="flex flex-col md:flex-row items-center md:items-start gap-10 max-w-5xl mx-auto mt-10 px-4"
>
  <!-- Imagen + badge -->
  <div class="flex flex-col items-center md:items-start">
    <div class="relative size-48 md:size-56">
      <div class="ring-wrapper">
        <img
          class="absolute inset-[4px] rounded-full object-cover w-[calc(100%-8px)] h-[calc(100%-8px)]"
          src="/foto.png"
          alt={personalImageAlt}
        />
      </div>
    </div>
    <div class="mt-4">
      <Badge>Disponible para trabajar</Badge>
    </div>
  </div>

  <!-- Texto + contactos -->
  <div
    class="flex flex-col items-center md:items-start text-center md:text-left max-w-xl"
  >
    <h1
      class="text-4xl font-bold tracking-tight text-black sm:text-5xl dark:text-white"
    >
      Hola, soy <span class="animated-gradient-text">Victor</span>
    </h1>

    
    <p 
      class="mt-6 text-lg sm:text-xl text-gray-800 dark:text-gray-300 [&>strong]:font-semibold leading-relaxed [&>strong]:text-blue-600 dark:[&>strong]:text-cyan-400">
      <span id="typewriter-text"></span><span id="typewriter-cursor" class="typewriter-cursor">|</span>
    </p>

    <nav class="flex flex-wrap gap-4 mt-8">
      <SocialPill
        href="https://mail.google.com/mail/?view=cm&to=vmsamur2007@gmail.com"
        target="_blank"
        rel="noopener noreferrer"
      >
        <MailIcon class="size-5" />
        Contáctame
      </SocialPill>

      <SocialPill
        href="https://www.linkedin.com/in/victor-moreno-931b42386/"
        target="_blank"
        rel="noopener noreferrer"
      >
        <LinkedInIcon class="size-5" />
        LinkedIn
      </SocialPill>

      <SocialPill
        href="https://github.com/victormoreno-2007"
        target="_blank"
        rel="noopener noreferrer"
      >
        <GitHub class="size-5" />
        GitHub
      </SocialPill>
    </nav>
  </div>
</section>
<script>
  function initTypewriter() {
    const el = document.getElementById('typewriter-text');
    const cursor = document.getElementById('typewriter-cursor');
    if (!el || !cursor) return;

    const text = 'Técnico en Programación de Software y Desarrollador Web. Me enfoco en crear soluciones tecnológicas eficientes, automatizar procesos y construir aplicaciones escalables. Me apasiona transformar ideas en código y aprender constantemente para aportar el mayor valor posible.';

    const boldStart = text.indexOf('crear soluciones');
    const boldEnd   = text.indexOf('aplicaciones escalables.') + 'aplicaciones escalables.'.length;

    let i = 0;
    el.innerHTML = '';

    const interval = setInterval(() => {
      i++;
      const current = text.slice(0, i);
      const before = current.slice(0, boldStart);
      const bold   = current.slice(boldStart, Math.min(i, boldEnd));
      const after  = current.slice(Math.min(i, boldEnd));

      if (i <= boldStart) {
        el.innerHTML = current;
      } else if (i <= boldEnd) {
        el.innerHTML = before + '<strong class="font-semibold text-blue-600 dark:text-cyan-400">' + bold + '</strong>';
      } else {
        el.innerHTML = before + '<strong class="font-semibold text-blue-600 dark:text-cyan-400">' + text.slice(boldStart, boldEnd) + '</strong>' + after;
      }

      if (i >= text.length) {
        clearInterval(interval);
        cursor.style.display = 'none'; 
      }
    }, 18); 
  }

  document.addEventListener('astro:page-load', initTypewriter);
  initTypewriter();
</script>
<style>
  .animated-gradient-text {
    background: linear-gradient(90deg, #3b82f6, #8b5cf6, #06b6d4, #3b82f6);
    background-size: 200% auto;
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: gradient-move 3s linear infinite;
  }

  @keyframes gradient-move {
    0% {
      background-position: 0% center;
    }
    100% {
      background-position: 200% center;
    }
  }
  .ring-wrapper {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  padding: 4px;
  background: linear-gradient(135deg, #3b82f6, #8b5cf6, #06b6d4, #3b82f6);
  background-size: 300% 300%;
  animation: ring-rotate 3s linear infinite;
  position: relative;
}

.ring-wrapper::before {
  content: '';
  position: absolute;
  inset: 4px;
  border-radius: 50%;
  background: white;
  z-index: 0;
}

:global(.dark) .ring-wrapper::before {
  background: #0f172a;
}

.ring-wrapper img {
  position: relative;
  z-index: 1;
}

@keyframes ring-rotate {
  0%   { background-position: 0% 50%; }
  50%  { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}
.typewriter-cursor {
  display: inline-block;
  width: 2px;
  background-color: currentColor;
  margin-left: 2px;
  animation: blink 0.8s step-end infinite;
  font-weight: 100;
}

@keyframes blink {
  0%, 100% { opacity: 1; }
  50%       { opacity: 0; }
}
</style>
